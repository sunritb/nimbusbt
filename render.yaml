services:
  - type: web
    name: nimbusbt
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: node src/server.js --host 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_ENV
        value: production
